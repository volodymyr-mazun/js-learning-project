МОДУЛЬ 1. ЗМІННІ ТА ТИПИ. ОСНОВИ ФУНКЦІЇ.

1. БАЗОВА ТЕРМІНОЛОГІЯ.
2. ЗМІННІ ТА ТИПИ ДАНИХ.
3. РЯДКИ.
   Конкатенація.
   Довжина та індексація.
4. ОПЕРАТОРИ ПОРЯВНЯННЯ.
   Арифметичні функції.
   Дробові числа.
5. ОСНОВИ ФУНЦІЇ.

МОДУЛЬ 2. РОЗГАЛУЖЕННЯ ТА ЦИКЛИ.

1. РОЗГАЛУЖЕННЯ.
   Оператор IF.
   Оператор IF...ELSE.
   Оператор ELSE...if.
   Торнарний оператор.
   Оператор SWITCH.
2. ЛОГІЧНІ ОПЕРАТОРИ.
   && Логічне "І".
   || Логічне "АБО".
   ! Логічне "НІ".
3. МЕТОДИ РЯДКІВ.
   slice()
   includes()
   indexOf()
   toUpperCase()
   toLoweCase()
   startsWith()
   endsWith()
   trim()
4. ЦИКЛИ.
   while
   do while
   for, Інкремент та Декремент

МОДУЛЬ 3. МАСИВИ ТА ФУНКЦІЇ.

1. МАСИВИ.
   Доступ та перевизначення.
   Довжина. Індекс останнього елемента.
2. МЕТОДИ МАСИВА.
   join()
   splite()
   slice()
   includes()
   indexOf()
   push()
   pop()
3. ІТЕРАЦІЯ ПО МАСИВУ.
   for (let i = 0; i < array.length; i++)
   for (let arr of array)
4. ФУНКЦІЯ
   Псевдомасив, Область видимості, Стек викликів, Функціональний вираз

МОДУЛЬ 4. ОБЄКТИ.

1. ОБЄКТИ.
   Доступ через крапку.
   Доступ через квадратні дужки.
   Додавання та зміна.
   Короткі та обчислювальні властивості.
2. ПЕРЕБІР ОБЄКТА.
   Цикл FOR...IN. Object.keys(). Object.values().
3. МАСИВ ОБЄКТІВ.
   Пошук обєкта за значенням властивості.
   Колекція значень властивості.
4. МЕТОДИ ОБЄКТА.
   Доступ до властивостей обєкта. THIS.
   Зміна за посиланням. Додавання.
   Масив обєктів. Перебір.
   Пошук та зміна обєкта в масиві.
5. СИНТАКСИС SPRED І REST.
   Залишкові параметри. Збір частини аргументів.
   Входження параметрів. Math.min(). Math.max.
   Створення масиву. Створення обєкта.

МОДУЛЬ 5. ПЕРЕБИРАЮЧІ МЕТОДИ МАСИВІВ.

1. КОЛБЕК-ФУНКЦІЯ.
   Інлайн-кллбеки.
   Метод FOREACH.
2. СТРІЛОЧНІ ФУНКЦІЇ.
   Синтаксис. Неявне перетворення.
   Колбеки.
3. МЕТОДИ MAP І FLATMAP.
   Чисті функції. Функції із побічними ефектами.
   Перебираючі методи.
   Масив обєктів.
4. МЕТОДИ FILTER І FIND.
   Методи на масиві обєктів.
5. МЕТОДИ EVERY, SOME, REDUCE.
   EVERY-задовільняють умову, елементи масиву.
   SOME-задовільняє умову, один елемент масиву.
   array.reduce((previousValue, element, index, array), initialValue);
6. МЕТОД TOSORTED.
   Масив чисел. Масив рядків.
   .toSorted((a, b) => a - b);
   .toSorted((a, b) => a.localeCompare(b)); - зростання
   .toSorted((a, b) => b - a);
   .toSorted((a, b) => b.localeCompare(a)); - спадання
   Сортування обєктів.
   Ланцюжки методів.

МОДУЛЬ 6. ООП. КЛАСИ.

1.  КОНТЕКСТ ВИКОНАННЯ ФУНКЦІЇ.
    Ключове слово THIS.
    Глобальний контекст. Контекст методу обєкта.
    Методи CALL, APPLY, BIND.
    Стрілочна функція.
2.  ПРОТОТИПИ.
    Перевірка прототипу - isPrototypeOf.
    Власні та невласні прототипи - hasOwnProperty.
    Перебір власних властивостей.
    Ланцюжки прототипів.
3.  ООП.
    Процедурне програмування. Окремі функції та глобальні змінні.
    Обєктоно-орієнтоване програмування. Обєкти - основа.
    Клас - визначає структуру, поведінку та правила взаємодії із обєктом.
    Екземпляр - має властивості і методи що працюють на основі правил із класу.
    Інтерфейс - властивості та методи для роботи із екземпляром.
4.  КЛАСИ.
    Оголошення класу. Конструктор класу.
    Обєкт параметрів.
    Методи класу.
    Прототип екземпляра.
    Приватні властивості. Приватні методи(валідація - @).
    Геттери та сеттери (get, set).
    Статичні властивості. Статичні методи.
    Наслідування класів - extends.
    Конструктор дочірнього класу - super.
    Методи дочірнього класу.

МОДУЛЬ 7. DOM. ПОДІЇ.

1. ОБЄКТНА МОДЕЛЬ ДОКУМЕНТА.
   Java Script в браузері. ECMASCRIPT-стандарт, DOM, BOM.
   HTML-документ і DOM. HTML-парсер.
   Пошук елементів. querySelector(selector) / querySelectorAll(selector).
   Навігація по DOM. Предок. Нащадок. Батько. Дитина. Сусід.
2. ВЛАСТИВОСТІ ТА АТРИБУТИ.
   Доступ до властивостей. - document.querySelector(".");
   ВЛАСТИВІСТЬ TEXTCONTENT. Доступ та зміна.
   ВЛАСТИВІСТЬ STYLE.
   ВЛАСТИВІСТЬ CLASSLIST - масив класів:
   Метод classList.contains(className) - перевірка наявності класу.
   Метод classList.add(className) - додавання класу.
   Метод classList.remove(className) - видалення класу.
   Метод classList.toggle(className) - видалення/додавання класу.
   Метод classList.replace(oldClassName, newClassName) - заміна класу.
   ДОСТУП ДО АТРИБУТІВ.
   Метод element.hasAttribute(nameAttribute) - перевірка наявності атрибуту.
   Метод element.getAttribute(nameAttribute) - отримання значення атрибуту.
   Метод element.setAttribute(nameAttribute, value) - додавання атрибуту та значення атрибуту.
   Метод element.removeAttribute(nameAttribute) - видалення атрибуту.
   ВЛАСНІ АТРИБУТИ. Отримання та зміна значення.
3. СТВОРЕННЯ ТА ВИДАЛЕННЯ ЕЛЕМЕНТІВ.
   Створення елементів - document.createElement("");
   Додавання елементів - append, prepend.
   Видалення елементів - remove.
   ВЛАСТИВІСТЬ INNERHTML.
   МЕТОД INSERTADJACENTHTML.
4. ПОДІЇ.
   Метод addEventListener().
   Метод removeEventListener().
   Об'єкт події - event.
   Події клавіатури - keydown / keyup.
   Властивості - event.key / event.code.
