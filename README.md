МОДУЛЬ 1. ЗМІННІ ТА ТИПИ. ОСНОВИ ФУНКЦІЇ.

1. БАЗОВА ТЕРМІНОЛОГІЯ.
2. ЗМІННІ ТА ТИПИ ДАНИХ.
3. РЯДКИ.
   Конкатенація.
   Довжина та індексація.
4. ОПЕРАТОРИ ПОРЯВНЯННЯ.
   Арифметичні функції.
   Дробові числа.
5. ОСНОВИ ФУНЦІЇ.

МОДУЛЬ 2. РОЗГАЛУЖЕННЯ ТА ЦИКЛИ.

1. РОЗГАЛУЖЕННЯ.
   Оператор IF.
   Оператор IF...ELSE.
   Оператор ELSE...if.
   Торнарний оператор.
   Оператор SWITCH.
2. ЛОГІЧНІ ОПЕРАТОРИ.
   && Логічне "І".
   || Логічне "АБО".
   ! Логічне "НІ".
3. МЕТОДИ РЯДКІВ.
   slice()
   includes()
   indexOf()
   toUpperCase()
   toLoweCase()
   startsWith()
   endsWith()
   trim()
4. ЦИКЛИ.
   while
   do while
   for, Інкремент та Декремент

МОДУЛЬ 3. МАСИВИ ТА ФУНКЦІЇ.

1. МАСИВИ.
   Доступ та перевизначення.
   Довжина. Індекс останнього елемента.
2. МЕТОДИ МАСИВА.
   join()
   splite()
   slice()
   includes()
   indexOf()
   push()
   pop()
3. ІТЕРАЦІЯ ПО МАСИВУ.
   for (let i = 0; i < array.length; i++)
   for (let arr of array)
4. ФУНКЦІЯ
   Псевдомасив, Область видимості, Стек викликів, Функціональний вираз

МОДУЛЬ 4. ОБЄКТИ.

1. ОБЄКТИ.
   Доступ через крапку.
   Доступ через квадратні дужки.
   Додавання та зміна.
   Короткі та обчислювальні властивості.
2. ПЕРЕБІР ОБЄКТА.
   Цикл FOR...IN. Object.keys(). Object.values().
3. МАСИВ ОБЄКТІВ.
   Пошук обєкта за значенням властивості.
   Колекція значень властивості.
4. МЕТОДИ ОБЄКТА.
   Доступ до властивостей обєкта. THIS.
   Зміна за посиланням. Додавання.
   Масив обєктів. Перебір.
   Пошук та зміна обєкта в масиві.
5. СИНТАКСИС SPRED І REST.
   Залишкові параметри. Збір частини аргументів.
   Входження параметрів. Math.min(). Math.max.
   Створення масиву. Створення обєкта.
